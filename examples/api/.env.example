DATABASE_URL=postgresql://pgbase:pgbase@localhost:55432/pgbase

# Optional — defaults live in src/config/env.config.ts, which is also what validates them at boot.
# PORT=3001
# WEB_ORIGIN=http://localhost:3000
# PGBASE_SLOT=pgbase_example
# PGBASE_PUBLICATION=pgbase
